name: Infrastructure Review

on:
    pull_request:
        paths:
            - "**.tf"

jobs:
    ai-review:
        name: Review Terraform Changes
        uses: Sai9700128/AI_infra_reviewer/.github/workflows/review.yml@main
        secrets:
            ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
